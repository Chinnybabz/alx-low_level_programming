#include <stdio.h>

/**
 * main - main function consists of single digits to base 10
 *
 * Return: always o
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
